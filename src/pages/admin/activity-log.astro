---
import PreviewLayout from '../../layouts/PreviewLayout.astro';

const title = "Activity Log | Island Goodes Admin";
const description = "Admin activity and change history";
---

<PreviewLayout title={title} description={description}>
  <section class="log-hero">
    <div class="container">
      <div class="hero-content">
        <a href="/admin" class="back-link">&larr; Back to Admin</a>
        <h1>Activity Log</h1>
        <p>Track all admin changes and activities</p>
      </div>
    </div>
  </section>

  <section class="log-section">
    <div class="container">
      <div class="log-controls">
        <div class="filter-group">
          <label>Filter by Type:</label>
          <select id="typeFilter" class="select-input">
            <option value="all">All Activities</option>
            <option value="user">User Management</option>
            <option value="settings">Settings</option>
            <option value="roadmap">Roadmap</option>
            <option value="image">Image Changes</option>
            <option value="login">Logins</option>
          </select>
        </div>
        <button id="clearLog" class="btn btn-outline">Clear Log</button>
      </div>

      <div class="log-card">
        <div id="logEntries" class="log-entries">
          <div class="log-empty">No activity recorded yet.</div>
        </div>
      </div>

      <div class="log-info">
        <p>Activity is stored locally in your browser. Clearing browser data will remove the log.</p>
      </div>
    </div>
  </section>
</PreviewLayout>

<style>
  .log-hero {
    background: var(--gradient-primary);
    color: #fff;
    padding: 8rem 2rem 3rem;
  }

  .back-link {
    display: inline-block;
    color: rgba(255,255,255,0.7);
    font-size: 0.875rem;
    margin-bottom: 1rem;
    transition: color 0.2s;
  }

  .back-link:hover {
    color: #fff;
  }

  .log-hero h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }

  .hero-content > p {
    opacity: 0.9;
  }

  .log-section {
    padding: 2rem;
    background: var(--color-bg-alt);
    min-height: 60vh;
  }

  .log-controls {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .filter-group {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .filter-group label {
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--color-text);
  }

  .select-input {
    padding: 0.5rem 1rem;
    border: 2px solid #fff;
    border-radius: 8px;
    font-size: 0.9375rem;
    background: #fff;
    cursor: pointer;
    min-width: 180px;
  }

  .select-input:focus {
    outline: none;
    border-color: var(--color-primary);
  }

  .log-card {
    background: #fff;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.05);
    overflow: hidden;
  }

  .log-entries {
    max-height: 600px;
    overflow-y: auto;
  }

  .log-empty {
    padding: 3rem;
    text-align: center;
    color: var(--color-text-light);
  }

  .log-entry {
    display: flex;
    gap: 1rem;
    padding: 1.25rem;
    border-bottom: 1px solid var(--color-bg-alt);
    align-items: flex-start;
  }

  .log-entry:last-child {
    border-bottom: none;
  }

  .log-icon {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .log-icon.user { background: #E0F2FE; color: #0369A1; }
  .log-icon.settings { background: #F3E8FF; color: #7C3AED; }
  .log-icon.roadmap { background: #DCFCE7; color: #059669; }
  .log-icon.image { background: #FEF3C7; color: #D97706; }
  .log-icon.login { background: #F0FDF4; color: #16A34A; }

  .log-content {
    flex: 1;
  }

  .log-action {
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 0.25rem;
  }

  .log-details {
    font-size: 0.875rem;
    color: var(--color-text-light);
    margin-bottom: 0.5rem;
  }

  .log-meta {
    display: flex;
    gap: 1rem;
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .log-user {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .log-info {
    margin-top: 1.5rem;
    text-align: center;
  }

  .log-info p {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
  }

  @media (max-width: 768px) {
    .log-controls {
      flex-direction: column;
      align-items: stretch;
    }
    .filter-group {
      flex-direction: column;
      align-items: stretch;
    }
    .log-hero {
      padding: 6rem 1.5rem 2rem;
    }
  }
</style>

<script src="/js/shared-data.js"></script>
<script>
  async function getActivityLog() {
    try {
      return await window.SharedData.getActivityLog();
    } catch {
      return [];
    }
  }

  function formatDate(timestamp: string) {
    const date = new Date(timestamp);
    return date.toLocaleDateString('en-US', {
      month: 'short',
      day: 'numeric',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit'
    });
  }

  function getIcon(type: string) {
    const icons: Record<string, string> = {
      user: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
        <circle cx="9" cy="7" r="4"/>
      </svg>`,
      settings: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="3"/>
        <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/>
      </svg>`,
      roadmap: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <polygon points="12,2 2,7 12,12 22,7 12,2"/>
        <polyline points="2,17 12,22 22,17"/>
        <polyline points="2,12 12,17 22,12"/>
      </svg>`,
      image: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
        <circle cx="8.5" cy="8.5" r="1.5"/>
        <polyline points="21,15 16,10 5,21"/>
      </svg>`,
      login: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"/>
        <polyline points="10,17 15,12 10,7"/>
        <line x1="15" y1="12" x2="3" y2="12"/>
      </svg>`
    };
    return icons[type] || icons.settings;
  }

  async function renderLog(filter = 'all') {
    const container = document.getElementById('logEntries');
    if (!container) return;

    const log = await getActivityLog();
    const filtered = filter === 'all' ? log : log.filter((e: any) => e.type === filter);

    if (filtered.length === 0) {
      container.innerHTML = '<div class="log-empty">No activity recorded yet.</div>';
      return;
    }

    container.innerHTML = filtered.reverse().map((entry: any) => `
      <div class="log-entry">
        <div class="log-icon ${entry.type}">
          ${getIcon(entry.type)}
        </div>
        <div class="log-content">
          <div class="log-action">${entry.action}</div>
          ${entry.details ? `<div class="log-details">${entry.details}</div>` : ''}
          <div class="log-meta">
            <span class="log-user">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                <circle cx="12" cy="7" r="4"/>
              </svg>
              ${entry.admin || 'Unknown'}
            </span>
            <span>${formatDate(entry.timestamp)}</span>
          </div>
        </div>
      </div>
    `).join('');
  }

  // Filter change
  document.getElementById('typeFilter')?.addEventListener('change', async (e) => {
    const filter = (e.target as HTMLSelectElement).value;
    await renderLog(filter);
  });

  // Clear log
  document.getElementById('clearLog')?.addEventListener('click', async () => {
    if (confirm('Are you sure you want to clear all activity logs?')) {
      await window.SharedData.clearActivityLog();
      await renderLog();
    }
  });

  // Initial render
  renderLog();
</script>
